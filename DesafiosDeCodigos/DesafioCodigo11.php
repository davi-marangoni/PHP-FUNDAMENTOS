<?php
    $t = intval(rtrim(fgets(STDIN)));
    for($i = 0; $i < $t; $i++)
    {
        [$a, $b, $n] = array_map('intval', explode(' ', rtrim(fgets(STDIN))));
        
        //nao entendi esse ¯\_(ツ)_/¯¯
 }